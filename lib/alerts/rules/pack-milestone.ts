import { type AlertInput } from '../types'

// Stub — pack milestone detection requires external pack data not yet available
export async function packMilestoneAlerts(): Promise<AlertInput[]> {
  return []
}
